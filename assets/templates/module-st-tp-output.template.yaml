module_st_testpoint_file:
  module: "MU"
  testpoints:
    - tp_id: "ADD_DYN_RS1_RANGE_001"
      category: "动态输入参数 TP"
      lifecycle_status: "complete"
      source: "ADD"
      parameter: "RS1"
      parameter_type: "reg"
      coverage_space: "range"
      verification_goal: "覆盖 ADD.rs1 参数编码范围。"
      input_basis: ["instruction_spec.ADD.rs1"]
      verification_scenario: "提交 rs1=x0~x31 的 ADD 指令。"
      operation: "ADD 使用 rs1 指定的源寄存器作为第一操作数。"
      expected_result: "全部合法 rs1 编码被接收并按输入规格执行。"
      coverage_strategy: {covergroup: "cg_add_rs1_range"}
      missing_information: []
      completion_criteria: []
    - tp_id: "MU_CFG_CTRL_MODE_001"
      category: "配置空间 TP"
      lifecycle_status: "draft"
      source: "MU"
      parameter: null
      parameter_type: null
      coverage_space: null
      verification_goal: "覆盖 CTRL.mode 合法配置空间。"
      input_basis: ["register_spec.CTRL.mode"]
      verification_scenario: "对 CTRL.mode 写入 normal、debug、perf。"
      operation: null
      expected_result: "按输入规格生效。"
      coverage_strategy: null
      missing_information: ["CTRL.mode 到 HDL signal 的映射", "有效采样事件"]
      completion_criteria: ["提供 HDL 映射、采样事件和 covergroup 名称后转为 complete"]
  blocked_items:
    - category: "Debug 能力 TP"
      source: "MU"
      blocking_reason: "未提供 debug 命令和状态定义，无法定义验证场景。"
      required_input: ["debug spec"]
  missing_input_report: []
  completeness_review:
    note: "只审查既有 TP inventory，不生成新 TP。"
    categories:
      - {category: "配置空间 TP", status: "draft", existing_tp_ids: ["MU_CFG_CTRL_MODE_001"], finding: "缺 HDL 映射和采样事件。"}
      - {category: "动态输入参数 TP", status: "covered", existing_tp_ids: ["ADD_DYN_RS1_RANGE_001"], finding: "已覆盖 rs1 编码范围。"}
